node_modules/
.DS_Store
*.log
.idea/
.vscode/
*.tsbuildinfo

# Local machine notes for agents — setup is per-machine, not shared
AGENTS.md

# Local planning/tracker artifacts + pi session state — never shipped
.wayfinder/
.pi/
